# Application dependencies. Upper bounds limit untested major/API changes;
# install with installer.py so PyTorch uses the correct CPU/CUDA wheel index.
librosa>=0.11,<2
numpy>=2.1,<3
openai>=1.109.1,<4
opencv-python>=4.11,<6
openpyxl>=3.1.5,<4
pandas>=2.2.3,<4
pydub>=0.25.1,<0.26
PyYAML>=6.0.3,<7
requests>=2.32.5,<3
spacy>=3.8.7,<3.9
streamlit>=1.49.1,<2
streamlit-searchbox>=0.1.24,<0.2
yt-dlp>=2026.6.9
json-repair>=0.58.6,<1
ruamel.yaml>=0.19.1,<0.20
InquirerPy>=0.3.4,<0.4
autocorrect-py>=2.14,<3
edge-tts>=7.2.8,<8

# Default local ASR: Qwen3-ASR + Qwen3-ForcedAligner (core/asr_backend/qwen_asr_local.py).
# Apple Silicon uses MLX (mlx-audio); every mlx-audio release with Qwen3-ASR needs
# Transformers 5 / huggingface-hub 1. Windows and Linux use the official qwen-asr
# package, which pins Transformers 4.57.6 (hub <1); the marker also matches Intel
# Macs, which installer.py rejects (no PyTorch 2.8 macOS x86_64 wheels). WhisperX is not an
# installer option and is not listed here; see docs/pages/docs/whisperx-manual.*.md.
qwen-asr==0.0.6; sys_platform != 'darwin' or platform_machine != 'arm64'
transformers>=4.57.6,<5; sys_platform != 'darwin' or platform_machine != 'arm64'
huggingface-hub>=0.36.2,<1; sys_platform != 'darwin' or platform_machine != 'arm64'
mlx-audio>=0.5.5,<0.6; sys_platform == 'darwin' and platform_machine == 'arm64'
transformers>=5.14,<6; sys_platform == 'darwin' and platform_machine == 'arm64'
huggingface-hub>=1,<2; sys_platform == 'darwin' and platform_machine == 'arm64'
# mlx-audio's forced aligner imports these lazily for Japanese/Korean but does not
# declare them; same pins as qwen-asr 0.0.6.
nagisa==0.2.11; sys_platform == 'darwin' and platform_machine == 'arm64'
soynlp==0.0.493; sys_platform == 'darwin' and platform_machine == 'arm64'
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0
audioop-lts>=0.2.2; python_version >= '3.13'

syllables>=1.1.5,<2
pypinyin>=0.55,<1
g2p-en>=2.1,<3
xmltodict>=1.0.4,<2

